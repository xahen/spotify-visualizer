export const Header = () => {
  return (
    <header className="bg-slate-800 w-screen h-12 fixed top-0 left-0">
      <nav>
        {/* navigation buttons */}
        <button>Home</button>
        <button>Stats</button>
      </nav>
    </header>
  );
};
